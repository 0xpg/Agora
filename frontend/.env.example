# Privy public app ID, from the Privy dashboard (Settings -> Basics -> App ID).
# Public by design — never put the Privy app secret or an authorization key here.
VITE_PRIVY_APP_ID=

# Optional. Only needed once config/addresses.json holds more than one deployment.
VITE_CHAIN_ID=84532
